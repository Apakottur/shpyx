[build-system]
requires = [
    "setuptools",
    "wheel",
]
build-backend = "setuptools.build_meta"

[tool.black]

line-length=120
target-version = ['py39']
skip-magic-trailing-comma = true


[tool.isort]
line_length = 120
profile = 'black'
